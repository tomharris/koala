//database info
$db_type = mysql
$db_host = localhost
$db_name = pos
$db_user = pos
$db_pass = letmein_pos

//printer
$print_device = /dev/printer

//setup file
$pos_pathto_setup = pos_setup_$db_type$.sql

//backup info
$db_backup_method = dump
//$db_backup_dir = ./backup
$db_backup_dir = /Users/tom/Projects/PoS/dbs
$db_backup_filename = backup_$_input$_$currentdatetime$.sql

//mysql backup commands
$db_backup_cmd = mysqldump --quick --host=$db_host$ --user=$db_user$ --password=$db_pass$ $db_name$ $_input$
$db_restore_cmd = mysql --host=$db_host$ --user=$db_user$ --password=$db_pass$ $db_name$

//postgresql backup commands (no cmdline passwords; use a 'trust' account)
$db_superuser = postgres
//$db_backup_cmd = pg_dump --inserts --host=$db_host$ --username=$db_user$ <!--table=$_input$!> $db_name$
//$db_restore_cmd = psql -X -q -d $db_name$ -h $db_host$ -U $db_superuser$
//$db_backup_cmd = /opt/local/lib/postgresql80/bin/pg_dump --inserts --host=$db_host$ --username=$db_user$ <!--table=$_input$!> $db_name$
//$db_restore_cmd = /opt/local/lib/postgresql80/bin/psql -X -q -d $db_name$ -h $db_host$ -U $db_superuser$